class PathLayer {

    /**
	 * 初始化八方向连通参数
	 */
	public initBlocks(width: number, height: number, cellwidth: number, cellheight: number, mapTiles: Array<Array<number>>): void {

	}

	public removeUnWalk(x: number, y: number): void {

	}

	public addUnWalk(x: number, y: number): void {
		
	}

	public destroy(): void {

	}
}